import { Text, View } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const CreateUnlockPinScreen = () => {
  const {} = useNavigation();

  return (
    <View>
      <Text>CreateUnlockPinScreen</Text>
    </View>
  );
};

export default CreateUnlockPinScreen;
