import { Text, View } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const ReferralCodeSuccessfulScreen = () => {
  const {} = useNavigation();

  return (
    <View>
      <Text>ReferralCodeSuccessfulScreen</Text>
    </View>
  );
};

export default ReferralCodeSuccessfulScreen;
