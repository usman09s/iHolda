import { Text, View } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const DropOffLocationListScreen = () => {
  const {} = useNavigation();

  return (
    <View>
      <Text>Drop Off Locations List</Text>
    </View>
  );
};

export default DropOffLocationListScreen;
